{
  "name": "treachery-functions",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint . --ext .js --max-warnings 0"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.1.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}

